name: Keep Render App Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:        # allow manual run

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call API
        run: |
          curl -X GET "https://mkt-system.onrender.com/api/payments/970321112345"
